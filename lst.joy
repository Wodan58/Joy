(*
    This program builds a list of integers.
*)
LIBRA
  swoncat == swap concat;
  from-to ==
	[] cons [pop pop] swoncat
	[>] swap
	[[dup succ] dip]
	[cons]
	linrec;
  from-to-list == [] from-to.

1 43052 from-to-list.
